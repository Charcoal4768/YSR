mdoels:

products:
id -> int
image_url -> str url pointing to perm-url in bucket
name -> str
tags -> List[str,str]

accounts:
id -> int
name -> str
email -> str
pass -> str
phone -> str
address -> str

tags:
id -> int
name -> str

EXAMPLE: 
product_tags = db.Table(
    'product_tags',
    db.Column('product_id', db.Integer, db.ForeignKey('products.id'), primary_key=True),
    db.Column('tag_id', db.Integer, db.ForeignKey('tags.id'), primary_key=True)
)

https://youtu.be/KyPjE1Sn-Ts?si=iRKsnlOtm63TfpWn